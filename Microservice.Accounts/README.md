Microservice for Cuenta and Movimiento. Endpoints:
- /cuentas
- /movimientos

Async messaging with RabbitMQ was removed from this workspace. Accounts microservice exposes `/cuentas` and `/movimientos` endpoints.
